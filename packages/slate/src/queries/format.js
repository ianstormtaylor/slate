/**
 * Check whether a `format` is atomic, defaults to false.
 *
 * @param {Annotation|Decoration|Mark} format
 * @return {Boolean}
 */

export const isAtomic = (fn, editor) => () => {
  return false
}
