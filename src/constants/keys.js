/**
 * Constants for special keys used in the `onKeyDown` handler.
 *
 * @type {Object}
 */

const KEYS = {
  KEY_ENTER: 'enter',
  KEY_TAB: 'tab',
  KEY_BACKSPACE: 'backspace'
}

/**
 * Export.
 *
 * @type {Object}
 */

export default KEYS
